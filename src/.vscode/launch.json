{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Chrome",
            "type": "chrome",
            "request": "launch",
            "url": "https://localhost:5000",
            "webRoot": "${workspaceRoot}/src"
        },
        {
            "name": "Firefox",
            "type": "firefox",
            "request": "launch",
            "url": "https://localhost:5000",
            "webRoot": "${workspaceRoot}/src"
        }
    ]
}